train.ipynb

Change the parent directory at first.
Then, run the cells of the tokenizer one by one, in order.

evaluate.ipynb

Change the parent directory at first.
Then, run the cells of the tokenizer one by one, in order.

